Task to be done later:
1.Email validation (duplicacy) using ng-change (binding model and getting data on every alteration in model) call function which "gets" data from node, which "gets" 
  data from db and send particular response to angular and render the validation on the webpage.
2.